EXPO_PUBLIC_DATA_MODE=local
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Set EXPO_PUBLIC_DATA_MODE=supabase only after migrations are applied and the
# Supabase URL/anon key are filled in. Missing credentials fall back to local mode.
